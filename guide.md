# AI小说安卓App 研发端Harness Engineering搭建方案

**核心定位**：面向**安卓前端界面开发+语音功能+AI创作模块**的全流程研发驾驭体系，不是管控AI写小说，而是**管控App研发全过程**——统一规范、自动化校验、降低协作成本、加速上线，解决前端混乱、语音bug、AI接口对接难、迭代失控等问题。

**Claude Code 适配结论**：**完全可以用 Claude Code 基于这套 Harness 持续迭代开发**，这套标准化、强约束、有闭环的研发体系，恰好是 Claude Code 高效协作的最佳载体——既能发挥Claude代码生成、调试、优化的能力，又能通过Harness杜绝代码混乱、违规开发、质量失控，实现人机协同快速迭代。

# 一、核心研发边界与规则前置（P0必做·定开发基调）

目标：给安卓开发划定硬规范，杜绝界面混乱、代码冗余、权限滥用、接口不统一，实现多人/单人均可快速开发。

## 1. 编写《安卓App研发Agent手册》（AGENTS.md）

内置到项目根目录，作为开发唯一准则，支持AI辅助开发、代码审查、自动化构建，核心覆盖：

- **安卓前端界面规范**：UI组件标准、页面路由规则、布局约束、配色/字体统一、控件命名规范、屏幕适配规则（适配不同安卓机型/横竖屏）

- **语音模块开发规范**：语音识别SDK接入标准、麦克风权限申请流程、语音指令映射规则、语音反馈话术、异常容错逻辑

- **AI创作模块规范**：AI接口请求/响应格式、上下文存储规则、断点续创逻辑、生成超时/失败兜底策略

- **核心业务流程规范**：小说新建/删除/进入创作空间的页面跳转、数据流转、状态管理、缓存规则

- **安卓工程规范**：包名结构、代码分层（MVVM/MVP）、资源文件命名、Git提交规范、版本号规则

## 2. 研发权限与安全护栏（安卓专属）

- **权限最小化**：仅申请`RECORD_AUDIO`（语音）、`INTERNET`（AI接口）、`WRITE_EXTERNAL_STORAGE`（缓存草稿），禁止冗余权限

- **代码安全约束**：禁止硬编码API密钥、禁止明文存储用户数据、禁止UI线程做耗时操作（语音识别/AI生成）

- **资源管控**：限制图片/音频资源大小、禁止冗余布局、自动回收语音/AI模块内存，防止安卓OOM崩溃

## 3. 研发Agent职责拆分（分工明确，加速开发）

- **前端界面Agent**：负责安卓XML布局、UI组件渲染、页面跳转、状态绑定，严格遵循UI规范

- **语音交互Agent**：负责SDK接入、指令识别、权限申请、语音播报，屏蔽底层硬件差异

- **AI接口Agent**：负责接口封装、参数校验、超时重试、上下文管理，对接AI创作服务

- **校验质检Agent**：负责代码规范检查、UI适配校验、功能闭环测试、bug拦截

# 二、安卓项目文件架构设计（Harness强制规范）

目标：统一目录结构、分层解耦、便于Claude Code精准定位模块，杜绝文件散乱、路径混乱，采用**MVVM标准分层+业务模块化+文档固化**架构，适配语音+AI小说创作场景，新增docs模块实现设计信息、任务信息持久化。

```plaintext
app/
├── src/main/
│   ├── AndroidManifest.xml       # 权限配置、页面注册（Harness管控权限白名单）
│   ├── java/com/xxx/novelapp/
│   │   ├── base/                  # 基类封装（BaseActivity/BaseFragment）
│   │   ├── ui/                    # 前端界面层（严格按页面拆分）
│   │   │   ├── home/              # 首页（小说列表）
│   │   │   ├── create/            # 新建小说页面
│   │   │   ├── delete/            # 删除确认模块
│   │   │   └── editor/            # 创作空间页面
│   │   ├── viewmodel/             # MVVM状态管理（业务逻辑隔离）
│   │   ├── model/                 # 数据模型（小说实体、语音指令实体）
│   │   ├── voice/                 # 语音模块（识别、播报、指令映射）
│   │   ├── ai/                    # AI接口模块（请求、重试、上下文）
│   │   ├── utils/                 # 工具类（缓存、适配、日志）
│   │   └── harness/               # Harness核心管控（校验、护栏、质检）
│   └── res/
│       ├── layout/                # XML布局文件（命名统一规范）
│       ├── mipmap/                # 图标资源
│       └── values/                # 字符串、样式、尺寸（统一配置）
├── docs/                          # 固化文档模块（Harness强制归档）
│   ├── design/                    # 设计信息固化
│   │   ├── ui.md                   # 界面设计规范、原型说明
│   │   ├── api.md                  # AI接口对接文档
│   │   └── voice.md                # 语音交互流程设计
│   ├── task/                       # 任务信息固化
│   │   └── task_records.json       # 全量任务记录（唯一JSON文件）
│   └── README.md                   # 文档索引、更新日志
├── AGENTS.md                      # 研发规则手册
├── lint.xml                       # 自动化Lint校验规则
├── build.gradle                   # 自动化构建配置
└── .gitignore                    # Git忽略文件配置

```

### docs模块核心规则（Harness强制管控）

- **design目录**：固化所有设计信息，包含UI规范、接口方案、语音交互逻辑，禁止随意修改，变更需走审批并记录版本

- **task_records.json（唯一任务文件）**：所有研发任务、迭代记录、bug修复、Claude生成任务全量归档，字段固定、格式统一，Harness自动同步写入

- **架构管控红线**：严禁docs文件乱放、严禁修改task_records.json字段结构、严禁手动篡改任务数据，新增任务必须通过Harness闭环写入

- **优势**：实现研发信息可追溯、任务进度可量化，适配Claude Code协同开发，便于复盘和迭代管控

### task_records.json 标准字段模板

```json
[
  {
    "task_id": "T20260321001",
    "task_type": "功能开发/BUG修复/迭代优化",
    "task_desc": "语音新建小说功能开发",
    "module": "voice/ui",
    "executor": "Claude Code/人工",
    "status": "待执行/进行中/已完成/驳回",
    "create_time": "2026-03-21 14:30:00",
    "finish_time": "2026-03-21 16:00:00",
    "check_result": "通过/不通过",
    "remark": "Harness自动化校验通过"
  }
]

```

# 三、核心研发Harness搭建（P0必做·安卓前端+功能一体化）

目标：搭建**自动化+标准化**的开发流水线，前端界面、语音、AI模块可快速复用、一键调试，减少重复造轮子。

## 1. 安卓前端界面Harness（可视化+标准化）

- **组件化模板库**：提前封装通用UI组件（小说列表、新建弹窗、删除确认框、创作编辑页），XML+代码复用，直接拖拽/调用生成界面

- **页面路由管控**：统一页面跳转规则，固定流程：`首页（小说列表）→ 新建小说页 → 创作空间页`，禁止乱跳页

- **状态管理约束**：采用MVVM架构，ViewModel隔离UI和业务逻辑，语音指令、AI生成状态实时同步到前端界面

- **适配自动化**：封装屏幕适配工具，自动适配安卓不同分辨率、刘海屏、折叠屏，无需手动调整布局

## 2. 语音功能Harness（全流程可控）

- **指令白名单模板**：预设语音指令映射表（新建小说、删除小说、进入创作、开始创作、暂停、撤销），直接对接识别引擎

- **权限申请Harness**：封装麦克风权限申请弹窗、拒绝兜底逻辑，自动引导用户开启权限，避免语音失效

- **语音状态同步**：语音识别中/识别成功/识别失败的界面动效、语音播报，统一封装调用

- **离线语音兜底**：本地缓存常用指令，无网络时也能响应基础操作（新建/删除/进入创作空间）

## 3. 核心业务Harness（新建/删除/创作空间）

- **新建小说流程**：前端弹窗输入名称（语音/手动）→ 名称校验 → 创建本地文件夹/数据库记录 → 跳转创作空间，全程自动化

- **删除小说流程**：语音/手动触发 → 二次确认弹窗 → 软删除（标记隐藏，保留7天） → 更新前端列表，防止误删

- **创作空间隔离**：每篇小说独立数据存储（SQLite），上下文、草稿、进度分离，进入对应空间自动加载数据

- **数据持久化**：封装本地缓存工具，自动保存创作草稿，断电/退出不丢失，重启恢复进度

## 4. AI创作接口Harness（对接无压力）

- **统一接口封装**：封装AI生成请求、取消、重试方法，前端只需调用，无需关心底层网络

- **超时熔断机制**：AI生成超时自动取消，前端提示重试，避免卡死

- **上下文管理**：自动拼接历史章节、人设、大纲，传递给AI接口，保证创作连贯性

# 四、Claude Code 协同迭代流程（基于Harness落地）

依托现有Harness规则，搭建**“人工指令→Claude生成→自动化校验→人工终审→代码入库+任务归档”**的稳定迭代链路，全程同步写入docs/task_records.json，实现任务全追溯：

## Step1：前置配置（让Claude对齐Harness规则）

- 把**AGENTS.md研发手册、docs设计文档、UI组件模板、语音指令白名单**上传给Claude，作为唯一生成依据

- 明确约束Claude：仅在Harness划定的规范内开发，禁止修改护栏规则、docs固化信息、task_records.json结构

- 限定技术栈：安卓原生（Kotlin/Java）、MVVM、SQLite、语音SDK标准对接方式，禁止引入无关依赖

## Step2：迭代开发执行链路（含任务归档）

1. **人工下达任务**：明确需求，Harness自动生成task_id，预写入task_records.json

2. **Claude生成代码**：严格按照docs设计+AGENTS.md规范，生成对应模块代码

3. **自动化质检拦截**：触发Lint、功能测试，结果同步更新至task_records.json

4. **人工终审**：核查代码合规性，终审结果写入任务JSON

5. **代码入库+任务闭环**：Git提交，Harness自动更新任务状态为完成

## Step3：分模块迭代适配场景

- **前端界面迭代**：Claude基于组件模板生成XML布局，Harness校验适配规范、命名规则

- **语音功能迭代**：Claude新增/优化语音指令，Harness校验权限逻辑、指令映射、离线兜底

- **核心业务迭代**：Claude调整新建/删除/创作空间逻辑，Harness校验数据隔离、缓存机制、流程闭环

- **AI接口迭代**：Claude优化接口对接代码，Harness校验超时熔断、上下文管理、参数格式

# 五、自动化研发闭环（细化落地版·P1进阶·提效降错）

目标：搭建**“任务创建→提交→校验→修复→构建→测试→入库→任务归档”**全自动化链路，同步固化任务信息至docs/task_records.json，无需人工值守基础校验，拦截90%低级bug。

## 1. 代码提交门禁（Pre-Commit 自动化校验）

- **执行工具**：Android Lint、ktlint（Kotlin）、Git Hooks

- **校验项**：代码格式、命名规范、语法错误、内存泄漏隐患、权限违规、硬编码敏感信息

- **执行逻辑**：代码提交前自动触发，不达标**阻断提交**，并生成结构化修复指令，推送给Claude Code自动修复，同步记录校验失败信息

- **Harness约束**：跳过提交门禁的代码禁止合并，严禁手动绕过校验

## 2. 自动化构建与打包

- **执行工具**：Gradle、Jenkins（可选）

- **流程**：代码入库→自动编译→自动签名→自动混淆→生成Debug/Release包

- **管控点**：版本号自动递增、依赖版本锁定、禁止引入未授权第三方库、多环境（测试/生产）自动切换

- **兜底**：构建失败自动发送告警，附上错误日志，触发Claude Code定位修复，同步记录构建状态

## 3. 自动化功能测试（UI+业务闭环）

- **执行工具**：Espresso（安卓UI测试）、JUnit（单元测试）

- **核心测试用例**：
- 语音指令识别（新建/删除/进入创作空间）
- 页面跳转与状态同步
- 小说新建/软删除/草稿持久化
- AI接口超时重试、语音权限申请

- **执行逻辑**：构建完成后自动跑测，生成测试报告，失败用例自动标记，推送Claude Code定向修复，测试结果写入任务JSON

## 4. 自修复与反馈闭环

- **轻量问题自动修复**：格式错误、导包冗余、资源命名不规范、简单语法bug，由Harness调用Claude Code一键修复

- **重量级问题人工介入**：业务逻辑bug、架构违规、安全风险，自动标注并推送开发者，修复后重新走校验流程

- **反馈沉淀**：修复记录同步更新至task_records.json，同步优化AGENTS.md和docs设计文档

## 5. 监控与告警闭环

- **执行工具**：Firebase、Bugly、自定义日志工具

- **监控维度**：App崩溃率、内存占用、语音识别成功率、AI接口响应耗时、Claude代码合规率

- **告警机制**：异常指标超标自动推送，Harness暂停高危迭代，先修复问题再继续开发，异常记录归档至docs

# 六、Claude Code 迭代避坑守则（Harness强制约束）

- 严禁Claude**自主修改Harness规则、AGENTS.md、docs固化文档、task_records.json结构**，所有调整必须人工审批

- 严禁Claude硬编码API密钥、敏感信息，代码必须符合安卓安全规范，Harness自动拦截违规代码

- 严禁脱离现有组件化、MVVM架构写代码，杜绝碎片化开发导致的项目混乱

- 高危操作（如删除逻辑、权限修改、数据库变更）必须二次校验，禁止Claude直接落地

- 每次迭代保留版本快照，借助Git快速回滚，防止Claude生成异常代码导致项目崩溃

# 七、兜底防护（P0必做·保障研发稳定）

- **代码回滚机制**：Git分支管控，出错可快速回滚到稳定版本

- **禁止自主修改规范**：所有Harness规则、docs文档、接口配置需人工确认，禁止AI/脚本擅自修改

- **界面降级兜底**：语音功能失效时，自动切换手动操作；AI接口失效时，展示本地草稿页面

# 八、快速开发验收指标（研发效率+AI协同）

|指标项|合格标准|
|---|---|
|前端界面开发效率|核心页面（新建/删除/创作）1天内完成|
|语音功能对接时长|≤2天|
|代码规范通过率|≥95%|
|安卓崩溃率|≤0.5%|
|功能闭环测试通过率|≥98%|
|Claude代码复用率|≥80%（减少人工手写）|
|AI代码修复通过率|≥70%（无需人工二次修改）|
|任务信息归档及时率|100%（全量任务自动写入JSON）|
**落地口诀**：规范定标准、文档固信息、任务留痕迹、自动化降错、全链路可控、AI协同增效
> （注：文档部分内容可能由 AI 生成）