# Claude Code 任务

## 当前状态
- 分支: feat/continue-tasks-v2
- Lint: ✅ 通过
- 单元测试: ✅ 通过（17个测试文件）
- 覆盖率: ❌ 0%（需要修复 JaCoCo 配置）

## 任务目标
参照 AGENTS.md 核心工作流程（第9.3节），完成以下步骤：

### Step 1: 修复 JaCoCo 覆盖率配置
**问题：**
- 测试运行成功（testDebugUnitTest 通过）
- 但 JaCoCo 覆盖率报告显示 0%
- executionData 文件未生成（app/build/jacoco/ 目录不存在）

**需要检查：**
1. app/build.gradle.kts 中的 `executionData` 配置
2. 检查 JaCoCo agent 是否正确运行
3. 确认 testDebugUnitTest 任务是否生成了 .exec 文件

### Step 2: 本地验证（AGENTS.md Step 10-11）
```bash
./gradlew lint test createDebugUnitTestCoverageReport
./gradlew assembleDebug
```

### Step 3: 提交代码
```bash
git add .
git commit -m "fix: 修复JaCoCo覆盖率配置"
git push -u origin feat/continue-tasks-v2
```

### Step 4: 创建 PR
```bash
gh pr create --base main --head feat/continue-tasks-v2 \
  --title "fix: 修复测试覆盖率配置" \
  --body "## 变更内容
- 修复 JaCoCo executionData 路径配置
- 覆盖率报告正常生成

## 检查清单
- [x] Lint 检查通过
- [x] 单元测试通过
- [x] 覆盖率 80%+
- [x] 本地构建验证"
```

### Step 5: 等待门禁并合并

## 参考文档
- AGENTS.md 第9.3节：核心开发流程
- AGENTS.md 第10.5节：测试覆盖率要求
